feat(phase4): 🎬 Implement comprehensive macro recording system

* 🎥 **Macro Recording Workflow:** Complete coordinate capture via mouse positioning and Enter key with Accept/Redo/Cancel flow; user moves mouse over game window and presses Enter to capture relative coordinates; out-of-bounds positions trigger automatic redo with warning
* 📋 **Action Type Support:** Implement all seven action types in macro sequences — MoveToPoint, MoveToRegion (Box/Circle), LeftClick, DragClick, Screenshot region (stub), Delay, and Loop with proper JSON schema validation
* 💾 **Macro File Management:** Add full CRUD operations — `Save-MacroFile`, `Get-MacroFile`, `Get-MacroFileList`, `Remove-MacroFile`, `Rename-MacroFile` with validation, filename convention (yyyyMMdd_HHmmss_<name>.json), and corruption handling; metadata includes version, creation/modification timestamps, and target window tracking
* ✅ **JSON Schema & Validation:** Define `MacroSchema.ps1` with complete validation framework — `Get-ValidMacroName`, `Test-MacroAction`, `Test-MacroFile` ensuring type safety, range constraints, loop nesting prevention, and action name uniqueness; all validators enforce schema constraints and provide human-readable error messages
* 🖱️ **Macro Execution Engine:** Implement `Invoke-MacroAction` and `Invoke-MacroSequence` with emergency stop integration checking `$script:EmergencyStopRequested` between every action; `Invoke-MouseDragClick` orchestrates mouse button down, Bezier drag path, and button up with proper `finally` block for safety
* 🎤 **Console App Screens:** Create `Show-RecordMacroScreen` for interactive recording workflow with step-by-step action building, `Show-RunMacroScreen` for macro execution with progress display and results, `Show-ManageMacrosScreen` for viewing macro details and editing (rename/reorder steps); all screens use `IAnsiConsole` injection for testability
* 🎛️ **ConsoleApp Bridge Enhancements:** Add `CreateEmptyTextPrompt` method to `ConsoleAppBridge.cs` for coordinate capture flow; refactor prompts across existing screens to use bridge factory methods for consistency; improve alternate screen handling
* 🧪 **Test Coverage:** Create comprehensive Pester tests covering all new functions — `MacroSchema.Tests.ps1`, `MacroFileManagement.Tests.ps1`, `Invoke-CaptureMousePosition.Tests.ps1`, `Invoke-MouseDragClick.Tests.ps1`, console app screen tests for recording/running/managing macros using `TestConsole` for UI validation; tests use `TestDrive:\` for file operations and mock all external dependencies
* 🐛 **Bug Fixes & Refactoring:** Fix coordinate rounding precision (4 decimal places); separate unit and integration tests; enhance error logging consistency; improve validation error messages for user guidance
* 📚 **Documentation:** Add `MacroFormat.md` with complete JSON schema reference, action type specifications, naming conventions, examples, and validation rules; update README with macro workflow documentation
* 📝 **Menu Integration:** Update main menu with "Manage macros" option; implement dispatch routing to recording/running/managing screens via `Invoke-InAlternateScreen`; update main entry point `Start-LastWarAutoScreenshot` with complete screen integration

function Show-WindowSelectionScreen {
    <#
    .SYNOPSIS
        Stub for the window selection screen. Implemented in Phase 4, task 4.1.

    .PARAMETER Console
        The Spectre.Console IAnsiConsole instance to use for rendering and input.
    #>
    [CmdletBinding()]
    param(
        [Parameter(Mandatory)]
        [Spectre.Console.IAnsiConsole]$Console
    )

    # Phase 4 stub — implementation in task 4.1
    return $null
}
